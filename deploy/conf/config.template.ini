[db]
NAME=${DB_NAME}
USER=${DB_USER}
HOST=db
PORT=5432
PASSWORD=${DB_PASSWORD}

[solr]
CORE_NAME=catalog_core
HOST=solr
PORT=8983

[django]
AUDIT_ACCOUNT_USERNAME=
RAVEN_PRIVATE_DSN=
RAVEN_PUBLIC_DSN=
SECRET_KEY=${SECRET_KEY}