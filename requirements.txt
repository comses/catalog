# DEPRECATED: this file is intentionally empty and must NOT be used as an
# active dependency source. All dependencies are declared in pyproject.toml
# and managed with uv (uv.lock).
#
#   uv sync     # install dependencies
#   uv lock     # regenerate the lockfile
#
# The deploy image (deploy/images/django.Dockerfile) no longer references
# this file; it is kept only for tools that still probe for its presence.
